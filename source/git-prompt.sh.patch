--- a/git-prompt.sh
+++ b/git-prompt.sh
@@ -204,13 +204,13 @@
 		"") # no upstream
 			p="" ;;
 		"0	0") # equal to upstream
-			p="=" ;;
+			p="" ;;
 		"0	"*) # ahead of upstream
-			p=">" ;;
+			p=" ↑" ;;
 		*"	0") # behind upstream
-			p="<" ;;
+			p=" ↓" ;;
 		*)	    # diverged from upstream
-			p="<>" ;;
+			p="" ;;
 		esac
 	else
 		case "$count" in
